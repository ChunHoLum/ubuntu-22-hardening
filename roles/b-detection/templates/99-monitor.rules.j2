# 1.Authentication & Privilege Files
# Monitor for any writes or attribute changes to files that control user access and privileges.
-w /etc/passwd -p wa -k auth_file_change
-w /etc/shadow -p wa -k auth_file_change
-w /etc/gshadow -p wa -k auth_file_change
-w /etc/group -p wa -k auth_file_change
-w /etc/sudoers -p wa -k auth_file_change
-w /etc/sudoers.d/ -p wa -k auth_file_change

# 2.Scheduled Tasks (Cron)
# Monitor for any writes or attribute changes to system-wide and user cron locations.
-w /etc/crontab -p wa -k cron_file_change
-w /etc/cron.d/ -p wa -k cron_file_change
-w /etc/cron.hourly/ -p wa -k cron_file_change
-w /etc/cron.daily/ -p wa -k cron_file_change
-w /etc/cron.weekly/ -p wa -k cron_file_change
-w /etc/cron.monthly/ -p wa -k cron_file_change
-w /var/spool/cron/ -p wa -k cron_file_change